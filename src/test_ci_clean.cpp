// Test file to verify CI workflow
// This file tests that code changes trigger the build job

#include <iostream>

int main() {
    std::cout << "CI workflow test - build job should run for this change" << std::endl;
    return 0;
}
