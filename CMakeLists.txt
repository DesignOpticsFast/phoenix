cmake_minimum_required(VERSION 3.26)
project(phoenix LANGUAGES CXX)

# Placeholder CMake configuration for Phoenix.
# Actual targets will be added when development begins.

message(STATUS "Phoenix build placeholder: no build targets defined.")
